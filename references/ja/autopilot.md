# 自動実行

`autopilot-run` は完了するかブロッカーに当たるまで進みます。

既定の出力は簡潔に保ちます。状態、ブロッカー、次の一手だけを返します。

testing 段階では、web と miniapp は DevTools 優先ブラウザ実行器、backend は API 実行器を優先します。

また、autopilot は状態更新だけでなく、既存コンテキストに基づく短い段階要約も文書へ補完します。
